SELECT 
    _ContactKey AS ContactKey,
    AccountId,
    Aluno_EAD__c,
    Anniversary_Date__c,
    beetalk__Phone__c,
    beetalk__PhoneOrMobilePhone__c,
    beetalk__SourceIdentification__c,
    Birthdate,
    Concordo_com_os_Termos_e_Condi_es__c,
    Contato__c,
    CreatedById,
    CreatedDate,
    Description,
    Email,
    et4ae5__HasOptedOutOfMobile__c,
    et4ae5__Mobile_Country_Code__c,
    FC_Cargo__c,
    FC_Departamento__c,
    FC_Email_pessoal__c,
    FC_Facebook__c,
    FC_Instagram__c,
    FC_LinkedIn__c,
    FC_Operadora_de_Celular__c,
    FC_Twitter__c,
    FirstName,
    HasOptedOutOfEmail,
    Id,
    IndividualId,
    Interesse_do_Cliente__c,
    IsPersonAccount,
    LastModifiedById,
    LastName,
    LeadSource,
    LicenciadaForm__c,
    Loca_o_com_Op_o_de_Compra__c,
    MailingCity,
    MasterRecordId,
    MobilePhone,
    Name,
    Observa_o__c,
    Oportunidade__c,
    OppET__c,
    OwnerId,
    Parceiro_Licenciado_c__c,
    Phone,
    pi__campaign__c,
    pi__comments__c,
    pi__conversion_date__c,
    pi__conversion_object_name__c,
    pi__conversion_object_type__c,
    pi__created_date__c,
    pi__first_activity__c,
    pi__first_search_term__c,
    pi__first_search_type__c,
    pi__first_touch_url__c,
    pi__grade__c,
    pi__Needs_Score_Synced__c,
    pi__notes__c,
    pi__score__c,
    pi__url__c,
    pi__utm_campaign__c,
    pi__utm_content__c,
    pi__utm_medium__c,
    pi__utm_source__c,
    pi__utm_term__c,
    ReportsToId,
    Segmento_Oportunidade__c,
    Send_Anniversary_Email__c,
    Send_Birthday_Email__c,
    Title,
    Todos_interesses__c
FROM 
    Contact_Salesforce
WHERE
    (
    SUBSTRING(Email, CHARINDEX('@',Email) + 1, LEN(Email)) LIKE '%yahoo.com%' 
    OR SUBSTRING(Email, CHARINDEX('@',Email) + 1, LEN(Email))  LIKE '%yahoo.com.br%'
    OR SUBSTRING(Email, CHARINDEX('@',Email) + 1, LEN(Email))  LIKE '%ymail.%'
    OR SUBSTRING(Email, CHARINDEX('@',Email) + 1, LEN(Email))  LIKE '%verizon.%'
    OR SUBSTRING(Email, CHARINDEX('@',Email) + 1, LEN(Email))  LIKE '%netscape.%'
    OR SUBSTRING(Email, CHARINDEX('@',Email) + 1, LEN(Email))  LIKE '%rogers.%'
    OR SUBSTRING(Email, CHARINDEX('@',Email) + 1, LEN(Email))  LIKE '%sky.%'
    OR SUBSTRING(Email, CHARINDEX('@',Email) + 1, LEN(Email))  LIKE '%aol.%'
    OR SUBSTRING(Email, CHARINDEX('@',Email) + 1, LEN(Email))  LIKE '%rocketmail.%'
    OR SUBSTRING(Email, CHARINDEX('@',Email) + 1, LEN(Email))  LIKE '%btinternet.%'
    OR SUBSTRING(Email, CHARINDEX('@',Email) + 1, LEN(Email))  LIKE '%talk21.%') 
